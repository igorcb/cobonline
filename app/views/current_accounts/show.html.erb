<p id="notice"><%= notice %></p>

<p>
  <strong>City:</strong>
  <%= @current_account.city.name %>
</p>

<p>
  <strong>Cost:</strong>
  <%= @current_account.cost.name %>
</p>

<p>
  <strong>Date ocurrence:</strong>
  <%= date_br(@current_account.date_ocurrence) %>
</p>

<p>
  <strong>Type launche:</strong>
  <%= @current_account.credito_debito %>
</p>

<p>
  <strong>Price:</strong>
  <%= @current_account.price %>
</p>

<p>
  <strong>Historic:</strong>
  <%= @current_account.historic %>
</p>

<%= link_to 'Edit', edit_current_account_path(@current_account), class: "btn btn-primary btn-sm btn-block" %>
<% if current_user.admin? %> 
  <%= link_to "Back", current_accounts_path, class: "btn btn-default btn-sm btn-block" %>
<% else %>
  <%= link_to "Back", index_user_operator_current_accounts_path, class: "btn btn-default btn-sm btn-block" %>
<% end %>
